encrypt:
  key-store:
    location: classpath:/sdgmkey.keystore
    password: sdgmskey
    alias: sdgmkey
    secret: sdgmskey
    enable: true


server:
  port: 8888

eureka:
  instance:
    hostname: config
    prefer-ip-address: false
    metadata-map:
      user.name: ${security.user.name}
      user.password: ${security.user.password}
  client:
    service-url:
      defaultZone: http://user:${REGISTRY_SERVER_PASSWORD:password}@registry:8761/eureka/

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tom110/springcloud-repo.git
          search-paths: /config-repo
          username: tom110
          password: litao413801
          skipSslValidation: true
          force-pull: true
  rabbitmq:
      host: rabbitmq
      port: 5672
      username: user
      password: password

security:
  user:
    name: user
    password: ${CONFIG_SERVER_PASSWORD:password}